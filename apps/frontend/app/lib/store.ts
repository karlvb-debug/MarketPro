'use client';

// ============================================
// Thin re-export — the store implementation lives in ./store/.
// Kept so existing imports from '(...)lib/store' keep working.
// ============================================

export * from './store/index';
